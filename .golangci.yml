# Small focused linter set so the CI signal stays meaningful.
version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - govet
    - ineffassign
    - staticcheck
    - unused

formatters:
  enable:
    - gofmt
